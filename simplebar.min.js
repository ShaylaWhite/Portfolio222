document.addEventListener('DOMContentLoaded', function() {
    var elements = document.querySelectorAll('.simplebar');
    elements.forEach(function(element) {
      new SimpleBar(element, { autoHide: true });
    });
  });
  